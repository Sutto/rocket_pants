source :rubygems

# Allow testing multiple versions with Travis.
gem 'rails', ENV['RAILS_VERSION'] if ENV['RAILS_VERSION'] && ENV['RAILS_VERSION'].length > 0

gem 'ci_reporter', '~> 1.6', :require => nil

gemspec